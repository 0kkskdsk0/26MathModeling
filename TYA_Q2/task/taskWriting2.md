# Task Card

## 1. Task
- 任务目的：
对 `TYA_Q2\writing\问题2正文.md` 做篇幅与写法修订，产出覆盖原文件的新版正文。修订的三个方向按优先级排列：
1. **压缩总篇幅**：全文从约 20,100 字符压缩到 12,000 至 14,000 字符。
2. **去工程化**：把工程实现细节从正文移除，正文只保留数学与结果。
3. **扩大模型篇幅占比**：模型建立整章（符号设定、模型假设、形式化建模、检验设计四小节合计）占修订后全文的 33% 至 40%，并扩充形式化建模小节的解释厚度。

修订参照两篇优秀论文的配比规律，原文位于 `E:\Zotero\storage\9N8UCK3E\C023\document.md` 与 `E:\Zotero\storage\D6UWQ6QR\C132\document.md`，执行时应先通读这两篇，重点体会其模型建立小节的展开方式。`TYA_Q2\writing\附录.md` 不修改，正文中指向附录的引用关系保持有效。

- 各节篇幅现状与目标（字符数为现有版本的实测值，目标值为修订后的上限或区间）：

| 小节 | 现状字符数 | 现状占比 | 目标 |
|---|---:|---:|---|
| 问题重述与问题分析 | 2,214 | 11.0% | ≤1,400 |
| 数据预处理整章 | 4,525 | 22.5% | ≤1,700，占比 ≤13% |
| 模型建立整章 | 6,942 | 34.5% | 占比 33%–40%，其中形式化建模小节扩充解释 |
| 模型求解 | 643 | 3.2% | ≤600，去掉工程细节 |
| 模型验证 | 902 | 4.5% | ≤500 |
| 敏感性分析 | 1,059 | 5.3% | ≤800 |
| 结论含三张表 | 3,803 | 18.9% | ≤2,800，三张表保留原样 |

- 数据预处理章的压缩口径：只留结论性产物。保留周同期结构结论、低负载日落点结论、最终特征集合、核权重公式、冻结带宽数值、平均能量分数的三方案对比结论、决策层回测的一句话结论。删除验证协议细节、消融实验的纳入条件与完整表格、带宽搜索过程、确认窗拆分的叙述、调参方向与适用边界小节。这些内容附录已有完整记录，正文删除后加一句"详见附录"即可。
- 去工程化的删除清单：求解器名称与接口细节、变量布局对象、冷启动的逐日口径细节、全年求解耗时、无前视泄漏检验的扰动参数、12 条断言的逐条编号与残差数值清单。求解节改写为按步骤编号的求解流程，只交代决策层、执行层、跨日链三步的先后关系。模型验证节压缩为三句话：断言核验全部通过、无前视泄漏检验通过、弃置占比的成因说明。`result2.xlsx` 是题目要求的成果文件名，可保留一处。
- 模型建立章的写法要求：每条目标函数与每组约束按"一句自然语言命名与动机、公式、其中逐符号解释、一句设计意图"的固定结构展开，解释文字约为公式本身的两到三倍，参照 C023 第 5.3 节与 C132 第 7.3 节的写法。符号表瘦身，只保留主模型实际使用的符号。
- 一处必须纠正的结构问题：最终采纳的模型是近视模型，但现版正文把终端价值项 $\theta_d^{\omega}$ 写进了主目标函数，符号表也收进了 $V_{d+1}(s)$、$\theta_d^{\omega}$、$a_{d,q}$、$b_{d,q}$。修订后主模型按近视呈现，目标函数不含终端价值项；$\theta$ 及其分段线性支撑只出现在"跨日近视假设的检验设计"小节，作为远视对照模型的构造引入，符号表相应删除这四个符号或在该小节内局部说明。
- 成功标准：
1. 全文总字符数落在 12,000 至 14,000 区间，数据预处理章不超过 1,700 字符，模型建立章占比在 33% 至 40% 之间；
2. 保留的所有数值与 `outputs/taskComposite/`、`outputs/taskCompositeFar/` 及现版正文逐项一致，压缩只允许删句删段，不允许改写任何数字；
3. 去工程化清单中的各项在正文中不可检出，模型建立章每条公式均符合"命名动机、公式、逐符号解释、设计意图"四段式结构；
4. 主模型目标函数不含 $\theta$，$\theta$ 仅在检验设计小节出现；表 1、表 2、表 3 三张表原样保留。

## 2. Context
- 当前阶段：问题二正文已由 `taskWriting.md` 任务完成首版，附录同步完成且质量合格，本次是纯修订任务，不涉及任何新计算，也不改动附录
- 现版正文的主要问题：总篇幅约 20,100 字符偏长；数据预处理章占 22.5%，高于两篇优秀论文中数据分析 5% 至 20% 的常见区间，且含有大量实验过程叙述；形式化建模小节的公式解释偏薄；求解与验证节带有求解器、布局对象、耗时等工程痕迹；结论章文字部分冗长
- 参考论文的核心配比规律：模型建立是单问题章节的第一大块，占 30% 至 45%；前期数据分析只留一张表或几条编号结论；公式一律用动机、解释、意图三段文字包裹；工程性产物一律进附录；结果与检验合计约 25% 至 40%
- 当前约束：附录不修改；所有数值以落盘文件为准，禁止四舍五入口径变化；图表引用 `assets/taskComposite/` 已有文件，可删减引用但不新建图；写作风格遵守项目 AGENTS.md，不用括号做补充说明；填报口径与结论判定不动摇，最终模型是近视模型，远视对照是负结果且只在既定口径下成立
- 当前非目标：不改 `Q2Project.md`；不改 `附录.md`；不做 LaTeX 排版；不补参考文献条目

## 3. Deliverables
- 必须产出的文件：
  - `TYA_Q2\writing\问题2正文.md`：覆盖原文件的修订版正文

## 4. Completion
- 交付物完成：修订版正文写完，章节框架与现版一致，附录引用关系有效
- 最小验证证据（smoke test）：用脚本统计各节字符数并核对满足第 1 条成功标准；抽取正文十个数值与 `outputs/taskComposite/tables.md`、`outputs/taskCompositeFar/comparison_far_vs_near.md` 核对一致；全文检索"HiGHS""linprog""布局对象""耗时"等工程词汇应为零命中（`result2.xlsx` 除外）；全文检索确认 $\theta$ 只出现在检验设计小节

## 5. Notes
- 易误解点：本任务是压缩与重配比，不是重写，模型、结论、数值、图表全部沿用，禁止借修订之机改动任何结论性表述；负结果的三层含义（假设不可检验的否证对象、终端价值无收益的结论、仅决策层远视口径的适用边界）在压缩后必须完整保留，可以缩短句子但不能丢失限定；弃置电量占比 17.61% 及其成因说明必须保留
- 可能犯错的部分：压缩数据预处理章时误删结构性证据中的关键数字（0.9722、85.28%、1.58 倍、551.071 kW、58.706%）；符号表瘦身时误删主模型实际使用的符号；把远视对照写成与近视模型并列的另一个主模型；表 2 填报实际执行路径的口径说明被删；压缩后公式失去逐符号解释，变成只有公式没有文字
